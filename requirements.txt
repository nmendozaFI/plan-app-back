# Transcripción — faster-whisper tiene wheels oficiales para Windows/macOS/Linux
faster-whisper
 
# Diarización — pyannote 3.x sí tiene wheels para Windows
pyannote.audio>=3.1.0
 
# torch CPU (sin CUDA) — la versión CPU-only pesa mucho menos
# En Windows ejecutá esto en lugar de la línea de abajo:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
torch
torchaudio
 
# HuggingFace (para descargar el modelo de pyannote)
huggingface_hub
 
# API y servidor
fastapi
uvicorn[standard]
python-multipart
 
# Claude
anthropic
 
# Utilidades
python-dotenv
rich
